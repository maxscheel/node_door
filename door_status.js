// not sure about the "submit" bit
module.exports.submit = doorStatus

function doorStatus() {
  var open = 
  var closed = 
  if {
    open == 1 | closed == 0
    status = open
  }
  else {
   open == 0 | closed == 1
   status = closed
  }
  else {
    status = unknown
  }
}
